services:
  - type: web
    name: warranty-management-dashboard
    env: python
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    pythonVersion: "3.12.10"
    envVars:
      - key: RENDER
        value: "true"
      - key: DATA_DIR
        value: /mnt/data
    disk:
      name: warranty-data
      mountPath: /mnt/data
      sizeGB: 5
